import { describe, expect, test } from 'vitest'
import { calculerHeure, calculerHeureInt } from '../calculerHeure'

describe('calculerHeure', () => {
    describe('calculerHeureInt', () => {
        test('calculerHeure', () => {
            expect(calculerHeureInt(1, 2, 1)).toEqual(0)
        });

        test('calculerHeure', () => {
            expect(calculerHeureInt(1, 2, 1)).toEqual(0)
        });

        test('calculerHeure', () => {
            expect(calculerHeureInt(1, 2, 1)).toEqual(0)
        });
    });

    describe('calculerHeure', () => {
        
    });

});